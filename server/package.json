{
  "name": "tortank-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "clean": "node -e \"import('better-sqlite3').then(({default:DB})=>{const db=new DB('data.db');['simulations','hypotheses','levers'].forEach(t=>db.prepare('DELETE FROM '+t).run());console.log('cleaned: simulations, hypotheses, levers');})\""
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "multer": "^2.1.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
